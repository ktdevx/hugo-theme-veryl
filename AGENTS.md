# AGENTS.md

## 概要

このリポジトリは、Hugo用テーマを開発するためのリポジトリです。

AIエージェントがコードを変更する際は、既存の設計・実装・命名規則を尊重し、必要最小限の変更を行ってください。

## 基本方針

- 既存の実装パターンを優先する。
- 同じ機能を実現するコードを重複して作らない。
- 変更は必要最小限にし、関係のないファイルを変更しない。
- 既存のユーザー設定やテーマの挙動を可能な限り維持する。
- 新しい依存関係は、必要性を十分に検討してから追加する。
- 生成物を直接編集しない。
- 既存の命名規則・ディレクトリ構成・コーディングスタイルに従う。

## リポジトリ構成

主要なディレクトリ・ファイルの役割は以下のとおりです。

| パス | 役割 |
| --- | --- |
| `layouts/` | Hugoテンプレート |
| `layouts/_default/` | ページ種別ごとの標準テンプレート |
| `layouts/partials/` | 共通パーツ・再利用可能なテンプレート |
| `layouts/shortcodes/` | Hugo Shortcode |
| `assets/` | Hugo Pipesで処理するCSS・JavaScript等 |
| `static/` | そのまま公開される静的ファイル |
| `i18n/` | 多言語対応用の翻訳ファイル |
| `data/` | Hugoで利用するデータファイル |
| `archetypes/` | コンテンツ生成用のアーキタイプ |
| `exampleSite/` | テーマの動作確認用サイト |
| `hugo.toml` | Hugoの設定 |

## Hugo

### バージョン

プロジェクトで指定されているHugoのバージョンを使用してください。

Hugoのバージョンによって利用可能なテンプレート関数や機能が異なるため、対応バージョンを超える機能を不用意に使用しないでください。

### テンプレート

- Hugo Templateの既存の記法・パターンに従う。
- 複雑な処理は可能な限りPartialへ分離する。
- 同じテンプレート処理を複数箇所にコピーしない。
- 既存のPartialが利用できる場合は、新しいPartialを作成せず既存のものを利用する。
- `.Params` に存在しない値を前提とした実装を避ける。
- オプションの設定値には適切なデフォルト値を設定する。
- 既存のテンプレート階層を尊重する。

## Front Matter

Front Matterで使用されるパラメータは、既存の互換性を維持してください。

新しいパラメータを追加する場合は、以下を行ってください。

1. パラメータの目的を明確にする。
2. 適切なデフォルト値を設定する。
3. 必要に応じて `exampleSite` に使用例を追加する。
4. 必要に応じてドキュメントを更新する。

既存のパラメータを、ユーザーへの告知なしに名前変更・削除しないでください。

## CSS

- 既存のCSS設計に従う。
- 既存のCSS変数・ユーティリティ・コンポーネントを優先して利用する。
- 同じスタイルを重複して定義しない。
- 不必要にセレクタの詳細度を高くしない。
- グローバルなスタイルを不用意に追加しない。
- レスポンシブデザインを維持する。
- ライトモード・ダークモードの両方を確認する。

## JavaScript

- JavaScriptでなくHTML/CSS/Hugoだけで実現できる場合は、JavaScriptを追加しない。
- 既存のJavaScript実装を優先して利用する。
- 不必要にJavaScriptフレームワークを導入しない。
- グローバル変数を不用意に追加しない。
- キーボード操作などのアクセシビリティを考慮する。
- JavaScriptが無効でも基本的なコンテンツが利用できる構造を可能な限り維持する。

## Shortcode

新しいShortcodeを追加する場合：

- 既存のShortcodeを確認する。
- 既存のShortcodeで代替できないか検討する。
- 必須パラメータが不足した場合の挙動を考慮する。
- ユーザーが利用するパラメータをドキュメント化する。
- 既存Shortcodeの仕様を不用意に変更しない。

## アクセシビリティ

UIを変更する場合は、以下を考慮してください。

- セマンティックなHTML
- キーボード操作
- フォーカス状態
- 適切なARIA属性
- 画像の代替テキスト
- 十分なコントラスト
- スクリーンリーダーでの利用

既存のアクセシビリティ対応を削除しないでください。

## レスポンシブデザイン

テーマのUIは、以下の画面サイズで適切に動作することを確認してください。

- モバイル
- タブレット
- デスクトップ

特定の画面サイズに依存した固定幅・固定高さを不用意に追加しないでください。

## 設定

テーマの設定項目を追加・変更する場合は、既存の設定との互換性を維持してください。

新しい設定を追加する場合：

- デフォルト値を設定する。
- `exampleSite` の設定例を更新する。
- 必要に応じてドキュメントを更新する。
- 実装内部の都合だけで不要な設定項目を公開しない。

## 機能ごとの編集場所

機能を追加・変更する場合は、まず既存の実装場所を確認してください。

| 機能 | 主な編集場所 |
| --- | --- |
| ヘッダー | `layouts/partials/header.html` |
| ナビゲーション | `layouts/partials/navigation.html` |
| フッター | `layouts/partials/footer.html` |
| 記事ページ | `layouts/_default/single.html` |
| 一覧ページ | `layouts/_default/list.html` |
| CSS | `static/css/` |
| JavaScript | `static/js/` |
| Shortcode | `layouts/shortcodes/` |
| 翻訳 | `i18n/` |

実際のリポジトリに存在するファイルを優先してください。

## 実装前の確認

コードを変更する前に、可能な限り以下を確認してください。

1. 同じ機能が既に実装されていないか検索する。
2. 関連するPartialを確認する。
3. 関連するCSS・JavaScriptを確認する。
4. 設定項目やFront Matterとの関連を確認する。
5. `exampleSite` への影響を確認する。

既存の実装を理解せずに、新しい仕組みを追加しないでください。

## Example Site

`exampleSite/` はテーマの動作確認および使用例を提供するために使用します。

テーマのユーザー向け機能を変更した場合は、必要に応じて `exampleSite/` に使用例を追加・更新してください。

特に以下の変更ではExample Siteへの反映を検討してください。

- 新しい設定項目
- 新しいShortcode
- 新しいページレイアウト
- 新しいUIコンポーネント
- 新しいFront Matterパラメータ

## テスト

変更後は、可能な限りHugoのビルドを実行してください。

```sh
hugo --gc --minify -s exampleSite --themesDir ../..
```

テンプレートを変更した場合は、以下を確認してください。

- Hugoのビルドが成功する。
- Template Errorが発生しない。
- ページが正常に生成される。
- CSS・JavaScriptなどのアセットが正常に生成される。
- 既存ページの表示が壊れていない。

UIを変更した場合は、可能な限り以下も確認してください。

- モバイル表示
- デスクトップ表示
- ライトモード
- ダークモード
- キーボード操作

## ドキュメント

以下の変更を行った場合は、必要に応じてドキュメントも更新してください。

- 新機能の追加
- 設定項目の追加・変更
- Shortcodeの追加
- 使用方法の変更
- インストール方法の変更
- 互換性に影響する変更

ドキュメントでは、ユーザーが利用する方法を優先して説明してください。

## Git

- 変更範囲を必要最小限にする。
- 関係のないファイルを変更しない。
- 生成物をコミットしない。
- `.gitignore` を変更して問題を隠蔽しない。
- 履歴を書き換えない。
- 既存のコミット方針がある場合は、それに従う。

## 禁止事項

以下を明確な理由なく行わないでください。

- 大規模なリファクタリング
- 不要な依存関係の追加
- 既存API・設定項目の削除
- 既存パラメータの名前変更
- 生成ファイルの直接編集
- 無関係なファイルのフォーマット変更
- UIフレームワークの導入
- 既存のアクセシビリティ機能の削除
- Hugoの最低対応バージョンを引き上げる変更

## 変更完了チェックリスト

変更を完了する前に、以下を確認してください。

- [ ] 既存の実装を確認した
- [ ] 既存コードを可能な限り再利用した
- [ ] 不要なファイルを変更していない
- [ ] Hugoのビルドが成功する
- [ ] 既存機能が壊れていない
- [ ] 新しい設定にデフォルト値がある
- [ ] 必要に応じてExample Siteを更新した
- [ ] 必要に応じてドキュメントを更新した
- [ ] UI変更の場合、レスポンシブ表示を確認した
- [ ] アクセシビリティを確認した
- [ ] 生成ファイルを直接編集していない
